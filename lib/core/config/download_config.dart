/// If set, the app will route web downloads through this proxy to bypass CORS
/// Example: 'http://localhost:8787/proxy' (Cloudflare Worker or local server)
/// Leave as null/empty to hit the origin directly.
const String? audioProxyBaseUrl = null;


